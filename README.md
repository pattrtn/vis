# Documentation for running the Streamlit application

## Command to run the Streamlit application

To run the Streamlit application, use the following command in your terminal:

```bash
streamlit run app.py
```